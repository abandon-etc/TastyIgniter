# 前台流程低风险验证

## 检查范围

本次检查目标是验证当前顾客前台主要流程是否能走到关键入口，包括首页、菜单页、购物车页、结账入口、预约页、后台登录页只读访问、`fr_CA` / `en_CA` 语言切换和 390px 移动端显示。

## 没有做的事

- 未登录后台。
- 未提交真实订单。
- 未提交真实预约。
- 未配置真实支付。
- 未配置真实邮件。
- 未录入真实菜单。
- 未使用真实顾客信息。
- 首次检查未写入数据库；Q-006 复测时仅临时修改本地开发数据库的营业时间 / collection 配置，并已按备份恢复。
- 未修改 TastyIgniter core。
- 未修改 `vendor/`。

## 首页检查结果

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 首页是否返回 200 | Yes | `http://127.0.0.1:8000` 可访问。 |
| 首页 CTA 是否可见 | Yes | `Commander` 和 `Réserver une fête` 可见。 |
| 点单按钮是否指向菜单页 | Yes | 指向 `http://127.0.0.1:8000/default/menus`。 |
| 预约按钮是否指向预约页 | Yes | 指向 `http://127.0.0.1:8000/default/reservation`。 |
| 语言切换是否可见 | Yes | `Français | English` 可见。 |
| `fr_CA` / `en_CA` 是否可切换 | Yes | `<html lang>` 可在 `fr_CA` 和 `en_CA` 之间切换。 |

## 菜单页检查结果

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 菜单页是否返回 200 | Yes | `http://127.0.0.1:8000/default/menus` 可访问。 |
| 是否显示测试或 demo 商品 | Yes | 可见当前 demo 商品，例如 `Puff-Puff`、`SCOTCH EGG` 等。 |
| 商品卡片是否正常 | Yes | 桌面端和 390px 移动端均显示正常。 |
| 商品图片是否正常 | Not applicable | 当前本地 demo 数据未渲染 `.menu-item-image` 商品图片元素；未发现图片破损。 |
| Pickup / Cueillette 是否显示正常 | Yes | `Cueillette` / Pickup 状态可见。 |
| 是否能把测试商品加入购物车 | Yes | 初次失败原因是测试时间不在营业时间内；临时打开本地营业时间后，demo 商品可加入购物车。见 Q-006。 |

## 购物车检查结果

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 购物车页是否返回 200 | Yes | `http://127.0.0.1:8000/cart` 可访问。 |
| 空购物车状态是否正常 | Yes | 显示空购物车提示。 |
| 是否显示商品、数量、价格和小计 | Yes | 临时营业状态下，购物车可显示测试商品、数量、价格、小计和订单总计。 |
| 是否能修改数量或移除商品 | Yes | 临时营业状态下，已验证商品数量可增加、减少，并可移除商品；移除后空购物车状态恢复。 |
| 是否提交订单 | No | 未点击最终下单按钮。 |

## 结账入口检查结果

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 空购物车访问 checkout 是否正常 | Yes | 访问 `http://127.0.0.1:8000/checkout` 后按系统现有流程回到菜单页。 |
| 有测试商品时是否能进入 checkout | Yes | 临时营业状态下，有测试商品时可进入 `http://127.0.0.1:8000/checkout`。 |
| checkout 是否显示 customer info / payment area | Yes | checkout 页面显示顾客信息字段、Pickup 信息和 payment area；未点击最终 `Confirm`。 |
| 是否填写真实姓名、电话、邮箱 | No | 未填写任何真实顾客信息。 |
| 是否点击 Place Order / Confirm Order / Pay | No | 未触发真实订单或真实支付。 |

## 预约页检查结果

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 预约页是否返回 200 | Yes | `http://127.0.0.1:8000/default/reservation` 可访问。 |
| 日期选择是否显示 | Yes | 页面包含 `date` 字段。 |
| 时间选择是否显示 | Yes | 页面显示时间选项。 |
| 人数选择是否显示 | Yes | 页面显示人数选择。 |
| 表单字段是否显示 | Partial | 初始步骤显示日期、人数和时间；未推进到最终顾客信息提交步骤。 |
| 是否提交预约 | No | 未点击最终预约提交按钮。 |
| 是否使用真实顾客信息 | No | 未输入真实顾客信息。 |

## 移动端检查结果

| 页面 | 结果 | 备注 |
| --- | --- | --- |
| 首页 390px | Pass | CTA 和语言切换可见，无明显破版。 |
| 菜单页 390px | Pass | 商品卡片和 Pickup / Cueillette 状态可见，无明显破版。 |
| 购物车页 390px | Pass | 空购物车状态可见，无明显破版。 |
| 预约页 390px | Pass | 日期、人数和时间选择可见，无明显破版。 |
| 语言切换 390px | Pass | `Français | English` 可见。 |

## 发现的问题

| 问题编号 | 发现位置 | 问题描述 | 是否影响上线 | 分类 | 建议下一步 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| Q-006 | 菜单页 / 购物车 / 结账入口 | 初次验证时当前本地时间不在营业时间内，前台显示 `CLOSED`，导致无法完成加入购物车和 checkout 验证。临时打开本地营业时间后，测试商品可加入购物车，数量可增加 / 减少，可移除商品，并可进入 checkout 表单。 | No | 前台流程 / 后台配置 | 已确认原因是测试时段不在营业时间内；测试后已恢复原始营业时间和 collection 配置。后续真实上线前仍需在准生产环境验证完整订单流程和支付配置。 | Resolved |

## Q-006 临时营业时间复测

复测日期：2026-07-08

复测方式：仅临时修改本地开发数据库的营业时间和 collection / Pickup 可用时间，让当前本地时间落在可下单窗口内。复测完成后，已按备份恢复原始配置。

备份文件：

- `.local/backups/q006-before-temp-open-20260708-102353.json`

临时修改范围：

- `working_hours`：仅临时将当前星期的 `opening` 和 `collection` 时间改为 `00:00-23:59`。
- `location_settings`：确认 `collection.is_enabled=true`，确认 `delivery.is_enabled=false`。
- `locations`：确认默认门店为启用状态。

复测结果：

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 菜单页是否不再显示 `CLOSED` | Yes | 临时营业状态下显示 `We are open`，Pickup / Cueillette 正常。 |
| 商品是否能加入购物车 | Yes | 使用当前 demo 商品完成加入购物车测试。 |
| 购物车是否显示商品、数量、价格和小计 | Yes | 可见商品名称、数量、价格、小计和订单总计。 |
| 是否能修改数量 | Yes | 已验证数量可增加和减少。 |
| 是否能移除商品 | Yes | 已验证商品可移除，空购物车状态可恢复。 |
| 是否能进入 checkout 表单 | Yes | 有测试商品时可进入 checkout 页面。 |
| checkout 是否显示顾客信息 / Pickup / payment area | Yes | 页面显示顾客信息字段、Pickup 信息和 payment area。 |
| 是否点击最终 `Confirm` / `Place Order` / `Pay` | No | 未提交订单，未触发真实支付。 |
| 是否提交预约 | No | 本次未提交预约。 |
| 是否使用真实顾客信息 | No | 未输入真实顾客信息。 |
| 是否恢复原始营业时间配置 | Yes | 已恢复每天 `12:00-22:00` 的 `opening` / `collection` 配置。 |
| Delivery 是否仍关闭或后置 | Yes | 恢复后 `delivery.is_enabled=false`。 |

## 上线前仍需处理事项

- Q-001：Carté Key / `fr_CA` Marketplace 翻译导入仍 Open。
- Q-005：完整站点翻译仍 Open。
- 真实菜单数据 Deferred。
- 支付未配置。
- 邮件服务未配置。
- 生日派对预约具体规则未最终配置。
- 税费 / 小费 / 支付设置仍需确认。
- 生产性能仍需验证。

## 是否建议继续部署准备

建议继续部署准备，但不要进入正式上线。

原因：

- 首页、菜单页、预约页、购物车页、后台登录页和语言切换基础入口可访问。
- 预约页初始步骤可显示。
- Q-006 已通过本地临时营业时间复测解决；真实上线前仍需在准生产环境验证完整订单流程和支付配置。
- 真实支付、真实邮件、真实菜单和生产备份仍未完成。
