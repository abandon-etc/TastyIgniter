# 菜单数据录入计划

## 当前状态

- 项目已进入菜单 / 菜品数据录入阶段。
- 本地 Docker 后台录菜响应慢，影响大量录入效率。
- 本地开发环境只保留少量测试菜品，用于验证菜单展示和基础流程。

## 性能诊断结论

- 数据库不是当前主要瓶颈。
- Docker 网络和基础服务正常。
- 慢主要来自 Windows bind mount 加 Laravel / TastyIgniter 大量小文件读取。
- 当前使用 `php artisan serve`，CLI OPcache 未生效。
- `APP_DEBUG=true` 和 Laravel 缓存未启用会加重慢感。
- 当前本地 Docker 性能不应直接等同于正式 Linux 生产环境性能。
- 正式部署到 Linux + Nginx / PHP-FPM + OPcache + `APP_DEBUG=false` + Laravel 缓存后，后台预计会明显改善。

## 当前决策

- 暂时跳过本地大量真实菜品录入。
- 不为了录菜重构本地 Docker 或迁移 WSL2。
- 继续推进功能、视觉、结账、预约和部署准备。
- 真实菜单数据等部署到正式或准生产环境后，再通过管理员后台录入。

## 本地仍需保留的测试数据

- 少量测试分类。
- 少量测试商品。
- 少量测试价格。
- 可选少量测试图片。
- 测试数据仅用于验证：
  - 菜单展示
  - 购物车
  - 结账入口
  - Pickup / Cueillette
  - 预约入口

## 部署后录入真实菜单的注意事项

- 生产数据库不能被重新初始化或覆盖。
- `storage` / uploads 必须持久化。
- 菜品图片上传必须正常。
- 管理员后台必须可访问。
- `.env` 生产配置不能被本地配置覆盖。
- 真实菜单录入后必须备份数据库和上传文件。

## 风险

- 如果生产环境部署脚本会重建数据库，真实菜单会丢失。
- 如果上传目录未持久化，菜品图片会丢失。
- 如果后台性能在准生产环境仍慢，需要再做生产性能诊断。
- 如果先在本地大量录菜，后续迁移图片和数据库可能增加复杂度。

## 下一步

- 继续做结账流程验证。
- 继续做预约流程验证。
- 做部署准备 checklist。
- 后续在准生产环境录入真实菜单。
