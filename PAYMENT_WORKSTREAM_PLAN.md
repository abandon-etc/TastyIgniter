# 板块四:收款系统工作流规划

日期:2026-08-28
状态:规划文档。**本批全部是方案与研究:不改代码、不改设置、不跑迁移;
每一项的执行在方案获批后各自另批。** D3C 剩余验收照常并行。

蓝图与步骤字母沿用 `BIRTHDAY_PAYMENT_ARCHITECTURE_DESIGN.md` 第 16 节的
PR 序列(A→B→C→D→F→E→G,H 先于 I,J 最后)。A、B、C(套餐/加购、
预订域与价格快照、时段锁)已完成并在册(`CLAUDE_HANDOFF.md` §7)。
本批产出:CI 启用方案(`CI_ENABLEMENT_PLAN.md`)、D 步与 F 步实施计划
(本文)、H 步法规研究(`QUEBEC_REFUND_CANCELLATION_RESEARCH.md`)、
基础设施两项方案(本文 §4)。

**排序约束**:CI 先于 D 步代码合并(交接文档 §10 早已把"实质性代码工作
前决定 CI"定为决策点);D 步迁移前先做一次按需备份(§4.1);E 步(测试
网关)最好落在独立测试库上(§4.2);F 完成前不得暴露任何顾客支付流。

## 1. D 步:共享交易层实施计划

范围(按设计文档 §7-§8,原文不复述,此处只定执行形状):
`App\Payments` 契约与服务、三张账本表模型、幂等服务、事件账本、退款
**接口壳**(实现等 H→I);不接任何真实网关。

### 1.1 迁移:加法式、可回退、作用于共享库

- 三张新表 `payment_transactions` / `payment_events` / `payment_refunds`,
  字段与约束按设计文档 §8(幂等键唯一、供应商事件/退款 ID 供应商域内
  唯一、payable 复合索引)。**只建新表,不触碰任何既有表**——这使运行中
  代码的读写路径零受影响,是"迁移作用于共享库"风险的第一道消解。
- **风险评估**:所有修订版共享一个库(结构性事实,在册),迁移一经执行
  即对正在服务主流量的修订版生效。因迁移为纯加表,主流量代码不知道也
  不引用这些表,预期影响为零;风险残余是迁移本身失败中断(锁、权限、
  磁盘)。缓解:迁移经一次性同镜像 Job 执行(既有模式),在与店主约定
  的时点;先 `migrate --pretend` 读回将执行的 SQL 入册;执行前后
  `SHOW TABLES` 与迁移状态读回;FP-1 对照包夹;执行前做一次按需 Cloud
  SQL 备份(§4.1),PITR 本已开启(7 天)。
- **回退路径**:`migrate:rollback` 精确回退该批次(空表阶段即为无损删
  三张表);表内一旦有数据即不再自动回退,改走设计文档 §18 的"调解不
  回滚"。`migrate:fresh/refresh/seed` 永远禁止(常设边界)。
- 表前缀沿用 `ti_`;迁移放项目命名空间,不动 vendor。

### 1.2 代码与测试

- 幂等:持久 `idempotency_key` 唯一约束 + 服务层"先查后建";事件账本:
  供应商事件 ID 唯一、重复事件为 no-op;退款壳:接口与状态机、无实现。
- 单元测试可无库跑(状态机、金额整数分、映射);需库的约束/并发测试
  依赖 CI 服务库(CI 先行的又一理由)或本地容器对。测试矩阵按设计文档
  §17 的 D 相关行。
- 审级:代码 PR 为 Level 1,**独立评审适用**(工作流 §7:支付邻域),
  停在 Ready to merge;迁移执行为 Level 2 且**单独审批**,与代码合并
  不捆绑。

## 2. F 步:登录注册 + 邮箱验证关卡实施计划

- 范围(设计文档 §14):支付前强制登录/注册;登录后把服务端预订/时段
  锁绑定到顾客;结账状态恢复;联系人快照;**邮箱验证关卡**按本批指示
  纳入 F(验证未过不得进入支付)。
- 执行首步是 vendor 盘点:igniter 用户扩展的注册/登录/激活(邮箱验证)
  能力与开关、主题已有 /login /register 页;能复用的不重写。
- 迁移:倾向**零迁移**(用既有 customers 表);确需字段则加法式一列,
  同 §1.1 的共享库纪律(加列属"改既有表",风险评估单独做:选可空列、
  无默认值重写,`--pretend` 读回)。
- 邮件依赖:验证邮件走既有邮件规则——测试修订版 `MAIL_MAILER=log` 或
  带 `MAIL_TEST_REDIRECT_TO` 的 smtp(2026-08-23 演练已闭合);生产
  传输是上线门禁,不在 F。法语:注册/验证页面与邮件属板块二"法语完整"
  范围,F 的验收含法语文案存在性检查。
- 测试:未登录/未验证不可达支付路径(服务端强制,非仅前端);状态恢复
  (登录前选择在登录后完整回来);验证邮件发出与链接激活(log/重定向
  收件箱);既有 Order 结账回归。
- 审级:Level 1 代码 + 独立评审(认证邻域,工作流 §7 明列),分格
  0% 副本验证 Level 2。

## 3. H 步:法规研究

已完成,交付为 `QUEBEC_REFUND_CANCELLATION_RESEARCH.md`(与税务调查同
规格:官方来源、URL 与读取日期、只记录不解读、开放问题清单)。I 步
(合规退款实现)等研究结论获店主专业意见确认后另批。

## 4. 基础设施两项

### 4.1 备份 + 恢复演练

现状(2026-08-28 只读读回):实例 `le-chateau-staging-mysql`
(db-custom-2-8192,ZONAL,10GB SSD 自动扩容,MySQL 8.4);**自动备份
已启用**(每日 15:00 UTC,保留 7 份),**PITR 开启**(binlog,事务日志
保留 7 天),最近三日自动备份均 SUCCESSFUL。缺口不是备份,是**从未演练
过恢复**(生产门禁明列 restore rehearsal)。

演练步骤(获批后执行,全程不触碰现实例):

1. 触发一次按需备份并读回 SUCCESSFUL(同时作为 D 步迁移的前置动作模板)。
2. 从该备份**恢复到新建临时实例**(恢复自真实备份工件,而非克隆——
   演练要证明的正是备份可恢复),实例名 `qa-restore-<date>`,同区同版本,
   可用更小规格以省费。
3. 验证:对照源库读回——表数、关键表行数(orders、locations、
   location_settings、working_hours)、抽样值(如 delivery 设置 JSON);
   记录恢复耗时(RTO 基线)与备份时点(RPO 说明:PITR 可到分钟级)。
4. 入册后删除临时实例(销毁性,**点名确认后执行**,describe 前后读回)。
5. 费用:临时实例存续期内按同规格计费(现全项目日均毛费 ~CA$5,演练
   控制在数小时,成本可忽略;仍在试用额度内,与 9 月中旬升级安排无涉)。
6. 频率:本次一回,生产上线前照此再演练一回。

**执行记录(2026-08-28,获批当日)**:按上列步骤完整执行一遍并全部通过
——备份 97 秒(id `1787958480451`)、建实例约 3 分钟、恢复 4 分 19 秒、
双实例套接字对照验证(表数、十表行数、三张配置表 CHECKSUM、抽样值)
全等;**实测 RTO 端到端约 10 分钟**(当前数据量),RPO=快照时点、PITR
可补至分钟级。细节在 `ADMIN_CONFIGURATION_TRACKER.md` 同日条目。临时
实例待点名确认后删除;生产上线前的再演练仍在计划内。

### 4.2 支付测试用独立数据库

动机:在册结构性限制——所有修订版共享一库,设置无法按副本隔离;支付
测试(E 步)要制造失败、改设置、造合成交易,不应与共享库互扰。

**方案甲(推荐):同实例新库。**

- 在现实例上新建数据库(如 `tastyigniter_paytest`),成本≈0。
- 初始化:从现库导出/导入一份(gcloud sql export/import 或 Job 内
  mysqldump),得到真实结构+设置+菜单的副本,数据全为既有合成/配置类;
  或空库跑全量迁移(但 fresh-install 迁移顺序缺陷在册,导出/导入更稳)。
- 接入:测试修订版把 `DB_DATABASE` 以**明文 env 覆盖**(其余凭据仍走
  Secret Manager;执行时先验证现有 DB 用户对新库的授权,不足则补 GRANT,
  属 Level 2 一次性动作)。
- 隔离边界:模式级隔离——设置、订单、迁移互不可见;**共享的仍有**:
  实例 CPU/IO(测试负载可能拖慢共享库,预备环境规模下可接受)、实例级
  维护窗口/重启、磁盘配额。规则:主流量修订版永不指向测试库;指向测试
  库的修订版必须 0% 流量 + URL 钉标签;Job 写库时显式点名目标库。
- D 步迁移可先在测试库彩排一遍再上共享库,把"共享库首迁"降为已演练
  动作。

**方案乙:新实例。**完全隔离(含故障域与维护窗口),但常驻第二实例
费用≈翻倍 SQL 开销,且多一套运维面。仅当方案甲出现真实互扰时升级。

**建议**:甲先行,E 步全程在测试库;乙留作升级路径。

**执行记录(2026-08-28,获批当日)**:方案甲已落地——
`tastyigniter_paytest` 建于共享实例并以镜像内 `mysqldump`(侦察确认
存在;应用用户全局权限,无需补授权)单执行内全保真初始化:90/90 表、
12/12 外键、八张关键表行数全等、抽样值与写后状态一致;守卫要求目标库
为空才执行。边界规则自此生效:主流量修订版永不指向测试库。细节在
`ADMIN_CONFIGURATION_TRACKER.md` 同日条目;D 步迁移彩排即可在此库进行。

## 5. 本批不做

不改代码、不建表、不跑迁移(含 --pretend 之外的一切)、不动工作流文件、
不启用 Actions、不建备份/实例/数据库、不改任何设置。以上每项执行动作
在其方案获批后各自另批。
